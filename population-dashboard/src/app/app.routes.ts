import { Routes } from '@angular/router';
import { PopulationComponent } from './components/population/population';

export const routes: Routes = [
  { path: '', component: PopulationComponent }  // Default route for the landing view
];
