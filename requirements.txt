# Core backend dependencies
fastapi
uvicorn[standard]
boto3
pandas
python-dotenv
requests

# Optional but useful dependencies for typing and async
typing_extensions
httpx

# Lightweight essentials
Jinja2
numpy

# Keep only minimal runtime utilities (no notebooks)
anyio
h11
sniffio
idna
certifi
charset-normalizer
urllib3
